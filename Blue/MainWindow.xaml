<winuiex:WindowEx
    xmlns:winuiex="using:WinUIEx"
    x:Class="Blue.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Blue"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    IsResizable="False"
    IsShownInSwitchers="False"
    IsMinimizable="False"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:icons="using:CubeKit.UI.Icons" 
    mc:Ignorable="d">
    <Grid x:Name="Background" Background="Transparent" BorderBrush="{ThemeResource MicaBorderBrush}" BorderThickness="0">
        <Canvas x:Name="LayoutCanvas" Background="Transparent">
            <Grid x:Name="CharacterButton" Width="100" Height="100" Canvas.Left="24" Canvas.Top="24" Background="Transparent" Tapped="Character_Tapped" PointerPressed="Character_PointerPressed" PointerMoved="Character_PointerMoved" PointerReleased="Character_PointerReleased" PointerCanceled="Character_PointerCanceled">
                <Grid.ContextFlyout>
                    <MenuFlyout x:Name="Flyout" Placement="Bottom" ShouldConstrainToRootBounds="False">
                        <MenuFlyout.SystemBackdrop>
                            <MicaBackdrop/>
                        </MenuFlyout.SystemBackdrop>
                        <MenuFlyout.MenuFlyoutPresenterStyle>
                            <Style TargetType="MenuFlyoutPresenter">
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Margin" Value="0, -8, 0, 0" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="CornerRadius" Value="8" />
                                <Setter Property="MaxWidth" Value="10000" />
                                <Setter Property="MaxHeight" Value="10000" />
                                <Setter Property="IsDefaultShadowEnabled" Value="True"/>
                            </Style>
                        </MenuFlyout.MenuFlyoutPresenterStyle>
                        <ToggleMenuFlyoutItem Text="Always on top" IsChecked="{x:Bind Assistant.IsPinned, Mode=TwoWay}">
                            <MenuFlyoutItem.Icon>
                                <icons:FluentIconElement Symbol="Pin20"/>
                            </MenuFlyoutItem.Icon>
                        </ToggleMenuFlyoutItem>
                        <MenuFlyoutItem Text="Collapse to character (click Blue to reopen)" Click="Hide_Click">
                            <MenuFlyoutItem.Icon>
                                <icons:FluentIconElement Symbol="EyeHide20"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem Text="Settings" Click="Settings_Click">
                            <MenuFlyoutItem.Icon>
                                <icons:FluentIconElement Symbol="Settings20"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem Text="Exit" Click="Exit_Click">
                            <MenuFlyoutItem.Icon>
                                <icons:FluentIconElement Symbol="Dismiss20"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </Grid.ContextFlyout>
                <Image Source="{x:Bind Assistant.CharacterImageSource, Mode=OneWay}" Height="100" Width="100"/>
            </Grid>
        </Canvas>
    </Grid>
</winuiex:WindowEx>
