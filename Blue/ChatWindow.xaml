<winuiex:WindowEx
    xmlns:winuiex="using:WinUIEx"
    x:Class="Blue.ChatWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Blue"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:controls="using:Blue.Controls"
    xmlns:icons="using:CubeKit.UI.Icons" 
    xmlns:classes="using:Blue.Core.Classes" 
    xmlns:helpers="using:Blue.Helpers" 
    xmlns:vmm="using:Blue.Core.ViewModels.Messages" 
    xmlns:messages="using:Blue.Controls.Messages" 
    xmlns:ToolkitControls="using:CubeKit.UI.Controls.Toolkit"
    IsResizable="False"
    IsShownInSwitchers="False"
    IsMinimizable="False"
    mc:Ignorable="d">
    <Grid Background="Transparent" BorderBrush="{ThemeResource MicaBorderBrush}" BorderThickness="0">
        <Grid.Resources>
            <DataTemplate x:Key="AssistantMessageTemplate" x:DataType="vmm:AssistantMessageViewModel">
                <messages:AssistantMessage ViewModel="{x:Bind }" Margin="0, 8, 0, 8"/>
            </DataTemplate>

            <DataTemplate x:Key="UserMessageTemplate" x:DataType="vmm:UserMessageViewModel">
                <messages:UserMessage ViewModel="{x:Bind }" Margin="0, 8, 0, 8"/>
            </DataTemplate>

            <DataTemplate x:Key="SystemMessageTemplate" x:DataType="vmm:SystemMessageViewModel">
                <Grid Margin="0,6,0,6" Padding="12,6">
                    <Grid Background="{ThemeResource MicaBlurBrush}" CornerRadius="8" Padding="12,8">
                        <TextBlock Text="{x:Bind MessageText, Mode=OneWay}" 
                                   Foreground="{ThemeResource SystemAccentColorLight2}"
                                   FontSize="12" FontStyle="Italic"
                                   TextWrapping="WrapWholeWords" 
                                   HorizontalAlignment="Center" TextAlignment="Center"/>
                    </Grid>
                </Grid>
            </DataTemplate>

            <helpers:MessageSelector x:Key="MessageTemplateSelector"
                AssistantMessageTemplate="{StaticResource AssistantMessageTemplate}"
                UserMessageTemplate="{StaticResource UserMessageTemplate}"
                SystemMessageTemplate="{StaticResource SystemMessageTemplate}" />
        </Grid.Resources>

        <Grid x:Name="ChatPanel" Width="360">
            <Grid.RowDefinitions>
                <RowDefinition Height="20" />
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto" />
                <RowDefinition Height="36" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" x:Name="DragHandle" Background="Transparent"
                  PointerPressed="DragHandle_PointerPressed"
                  PointerMoved="DragHandle_PointerMoved"
                  PointerReleased="DragHandle_PointerReleased"
                  PointerCanceled="DragHandle_PointerCanceled">
            </Grid>

            <ListView Grid.Row="1" ItemTemplateSelector="{StaticResource MessageTemplateSelector}" SelectionMode="None" ItemsSource="{x:Bind Assistant.MessagesVM, Mode=OneWay}">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsStackPanel ItemsUpdatingScrollMode="KeepLastItemInView" VerticalAlignment="Bottom"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Margin" Value="0,8,0,8" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemContainerTransitions>
                    <TransitionCollection>
                        <EntranceThemeTransition IsStaggeringEnabled="True" />
                        <AddDeleteThemeTransition />
                        <ContentThemeTransition />
                    </TransitionCollection>
                </ListView.ItemContainerTransitions>
            </ListView>

            <Grid Grid.Row="2">
                <ToolkitControls:DropShadowPanel BlurRadius="16.0" ShadowOpacity="0.43" IsMasked="True" CornerRadius="8" OffsetX="0.0" OffsetY="0.0" Color="Black">
                    <Grid ColumnSpacing="4" BorderBrush="{ThemeResource MicaBorderBrush}" Padding="8" BorderThickness="1" Background="{ThemeResource MicaBrush}" CornerRadius="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="auto" />
                        </Grid.ColumnDefinitions>
                        <ToolkitControls:DropShadowPanel Grid.Column="0" ShadowOpacity="0.4" Color="Black" OffsetX="4" CornerRadius="8" OffsetY="4">
                            <TextBox Text="{x:Bind Assistant.CurrentText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PreviewKeyDown="TextBox_PreviewKeyDown" Style="{ThemeResource GlowTextBox}" PlaceholderText="Ask me anything..." Width="278" MaxHeight="300" VerticalAlignment="Bottom" AcceptsReturn="True" TextWrapping="Wrap" MaxLength="1000"/>
                        </ToolkitControls:DropShadowPanel>
                        <Button Grid.Column="1" Command="{x:Bind Assistant.SendPromptCommand, Mode=OneWay}" Visibility="{x:Bind InvertBoolToVis(Assistant.SendPromptCommand.IsRunning), Mode=OneWay}" VerticalAlignment="Bottom" Style="{ThemeResource ToolbarButton}" ToolTipService.ToolTip="Send message">
                            <Button.KeyboardAccelerators>
                                <KeyboardAccelerator Modifiers="None" Key="Enter" />
                            </Button.KeyboardAccelerators>
                            <icons:FluentSymbolIcon Symbol="Send20"/>
                        </Button>
                        <Button Grid.Column="1" Command="{x:Bind Assistant.SendPromptCancelCommand, Mode=OneWay}" Visibility="{x:Bind BoolToVis(Assistant.SendPromptCommand.IsRunning), Mode=OneWay}" VerticalAlignment="Bottom" Style="{ThemeResource ToolbarButton}" ToolTipService.ToolTip="Stop response">
                            <Button.KeyboardAccelerators>
                                <KeyboardAccelerator Modifiers="None" Key="Enter" />
                            </Button.KeyboardAccelerators>
                            <icons:FluentSymbolIcon Symbol="Stop20Filled" />
                        </Button>
                    </Grid>
                </ToolkitControls:DropShadowPanel>
            </Grid>

            <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="4" HorizontalAlignment="Left" VerticalAlignment="Center">
                <Button Click="Settings_Click" Style="{ThemeResource ToolbarButton}" Height="32" Width="40" ToolTipService.ToolTip="Open settings">
                    <icons:FluentSymbolIcon Symbol="Settings20"/>
                </Button>
                <Button Click="Hide_Click" Style="{ThemeResource ToolbarButton}" Padding="0" Height="32" Width="40" ToolTipService.ToolTip="Collapse to character. Click Blue to reopen.">
                    <icons:FluentSymbolIcon Symbol="EyeHide20" />
                </Button>
                <ToggleButton IsChecked="{x:Bind Assistant.IsPinned, Mode=TwoWay}" Style="{ThemeResource PinToggleButton}" Height="32" Width="40" ToolTipService.ToolTip="Always on top"/>
                <ToggleButton x:Name="FollowToggle" Height="32" Width="40" Checked="FollowToggle_Checked" Unchecked="FollowToggle_Unchecked" Background="Transparent" BorderThickness="0" ToolTipService.ToolTip="Chat follows Blue">
                    <icons:FluentSymbolIcon x:Name="FollowIcon" Symbol="Link20"/>
                </ToggleButton>
                <Button Command="{x:Bind Assistant.RefreshChatCommand}" Style="{ThemeResource RefreshButton}" Height="32" Width="40" ToolTipService.ToolTip="Restart chat"/>
            </StackPanel>
        </Grid>
    </Grid>
</winuiex:WindowEx>
