<winuiex:WindowEx
    xmlns:winuiex="using:WinUIEx"
    x:Class="Clippy.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Clippy"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    IsResizable="False"
    IsShownInSwitchers="False"
    IsMinimizable="False"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:controls="using:Clippy.Controls"
    xmlns:icons="using:CubeKit.UI.Icons" 
    xmlns:classes="using:Clippy.Core.Classes" 
    xmlns:helpers="using:Clippy.Helpers" 
    xmlns:vmm="using:Clippy.Core.ViewModels.Messages" 
    xmlns:messages="using:Clippy.Controls.Messages" 
    xmlns:ToolkitControls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">
    <Grid x:Name="Background" Background="Transparent" BorderBrush="{ThemeResource MicaBorderBrush}" BorderThickness="0">
        <Grid.Resources>
            <DataTemplate x:Key="ClippyMessageTemplate" x:DataType="vmm:ClippyMessageViewModel">
                <messages:ClippyMessage ViewModel="{x:Bind }" Margin="0, 8, 0, 8"/>
            </DataTemplate>

            <DataTemplate x:Key="UserMessageTemplate" x:DataType="vmm:UserMessageViewModel">
                <messages:UserMessage ViewModel="{x:Bind }" Margin="0, 8, 0, 8"/>
            </DataTemplate>

            <DataTemplate x:Key="SystemMessageTemplate">
                <messages:SystemMessage/>
            </DataTemplate>

            <helpers:MessageSelector x:Key="MessageTemplateSelector"
                ClippyMessageTemplate="{StaticResource ClippyMessageTemplate}"
                UserMessageTemplate="{StaticResource UserMessageTemplate}"
                SystemMessageTemplate="{StaticResource SystemMessageTemplate}" />
        </Grid.Resources>

        <Canvas x:Name="LayoutCanvas" Background="Transparent">
            <Grid x:Name="ChatPanel" Width="360" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="36" />
                </Grid.RowDefinitions>

                <ListView Grid.Row="0" ItemTemplateSelector="{StaticResource MessageTemplateSelector}" SelectionMode="None" ItemsSource="{x:Bind Clippy.MessagesVM, Mode=OneWay}">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsStackPanel ItemsUpdatingScrollMode="KeepLastItemInView" VerticalAlignment="Bottom"/>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Margin" Value="0,8,0,8" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemContainerTransitions>
                        <TransitionCollection>
                            <EntranceThemeTransition IsStaggeringEnabled="True" />
                            <AddDeleteThemeTransition />
                            <ContentThemeTransition />
                        </TransitionCollection>
                    </ListView.ItemContainerTransitions>
                </ListView>

                <Grid Grid.Row="1">
                    <ToolkitControls:DropShadowPanel BlurRadius="16.0" ShadowOpacity="0.43" IsMasked="True" CornerRadius="8" OffsetX="0.0" OffsetY="0.0" Color="Black">
                        <Grid ColumnSpacing="4" BorderBrush="{ThemeResource MicaBorderBrush}" Padding="8" BorderThickness="1" Background="{ThemeResource MicaBrush}" CornerRadius="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="auto" />
                            </Grid.ColumnDefinitions>
                            <ToolkitControls:DropShadowPanel Grid.Column="0" ShadowOpacity="0.4" Color="Black" OffsetX="4" CornerRadius="8" OffsetY="4">
                                <TextBox Text="{x:Bind Clippy.CurrentText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PreviewKeyDown="TextBox_PreviewKeyDown" KeyUp="TextBox_KeyUp" Style="{ThemeResource GlowTextBox}" PlaceholderText="Ask me anything..." Width="278" MaxHeight="300" VerticalAlignment="Bottom" AcceptsReturn="True" TextWrapping="Wrap" MaxLength="1000"/>
                            </ToolkitControls:DropShadowPanel>
                            <Button Grid.Column="1" Command="{x:Bind Clippy.SendPromptCommand, Mode=OneWay}" Visibility="{x:Bind InvertBoolToVis(Clippy.SendPromptCommand.IsRunning), Mode=OneWay}" VerticalAlignment="Bottom" Style="{ThemeResource ToolbarButton}" ToolTipService.ToolTip="Send message">
                                <Button.KeyboardAccelerators>
                                    <KeyboardAccelerator Modifiers="None" Key="Enter" />
                                </Button.KeyboardAccelerators>
                                <icons:FluentSymbolIcon Symbol="Send20"/>
                            </Button>
                            <Button Grid.Column="1" Command="{x:Bind Clippy.SendPromptCancelCommand, Mode=OneWay}" Visibility="{x:Bind BoolToVis(Clippy.SendPromptCommand.IsRunning), Mode=OneWay}" VerticalAlignment="Bottom" Style="{ThemeResource ToolbarButton}" ToolTipService.ToolTip="Stop response">
                                <Button.KeyboardAccelerators>
                                    <KeyboardAccelerator Modifiers="None" Key="Enter" />
                                </Button.KeyboardAccelerators>
                                <icons:FluentSymbolIcon Symbol="Stop20Filled" />
                            </Button>
                        </Grid>
                    </ToolkitControls:DropShadowPanel>
                </Grid>

                <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="4" HorizontalAlignment="Left" VerticalAlignment="Bottom">
                    <Button Click="Settings_Click" Foreground="{ThemeResource SystemAccentColorLight2}" Style="{ThemeResource SettingsToolbarButton}" ToolTipService.ToolTip="Open settings"/>
                    <AppBarSeparator/>
                    <Button Click="Hide_Click" Style="{ThemeResource ToolbarButton}" Padding="0" Foreground="{ThemeResource SystemAccentColorLight2}" Height="32" Width="40" ToolTipService.ToolTip="Collapse to character. Click Blue to reopen.">
                        <icons:FluentSymbolIcon Symbol="EyeHide20" />
                    </Button>
                    <ToggleButton IsChecked="{x:Bind Clippy.IsPinned, Mode=TwoWay}" Style="{ThemeResource PinToggleButton}" Height="32" Width="40" ToolTipService.ToolTip="Always on top"/>
                    <Button Command="{x:Bind Clippy.RefreshChatCommand}" Style="{ThemeResource RefreshButton}" Height="32" Width="40" ToolTipService.ToolTip="Restart chat"/>
                </StackPanel>
            </Grid>

            <Grid x:Name="CharacterButton" Width="100" Height="100" Background="Transparent" Tapped="Character_Tapped" PointerPressed="Character_PointerPressed" PointerMoved="Character_PointerMoved" PointerReleased="Character_PointerReleased" PointerCanceled="Character_PointerCanceled">
                <Grid.ContextFlyout>
                    <MenuFlyout x:Name="Flyout" Placement="Bottom" ShouldConstrainToRootBounds="False">
                        <MenuFlyout.SystemBackdrop>
                            <MicaBackdrop/>
                        </MenuFlyout.SystemBackdrop>
                        <MenuFlyout.MenuFlyoutPresenterStyle>
                            <Style TargetType="MenuFlyoutPresenter">
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Margin" Value="0, -8, 0, 0" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="CornerRadius" Value="8" />
                                <Setter Property="MaxWidth" Value="10000" />
                                <Setter Property="MaxHeight" Value="10000" />
                                <Setter Property="IsDefaultShadowEnabled" Value="True"/>
                            </Style>
                        </MenuFlyout.MenuFlyoutPresenterStyle>
                        <ToggleMenuFlyoutItem Text="Always on top" IsChecked="{x:Bind Clippy.IsPinned, Mode=TwoWay}">
                            <MenuFlyoutItem.Icon>
                                <icons:FluentIconElement Symbol="Pin20"/>
                            </MenuFlyoutItem.Icon>
                        </ToggleMenuFlyoutItem>
                        <MenuFlyoutItem Text="Collapse to character (click Blue to reopen)" Click="Hide_Click">
                            <MenuFlyoutItem.Icon>
                                <icons:FluentIconElement Symbol="EyeHide20"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem Text="Settings" Click="Settings_Click">
                            <MenuFlyoutItem.Icon>
                                <icons:FluentIconElement Symbol="Settings20"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem Text="Exit" Click="Exit_Click">
                            <MenuFlyoutItem.Icon>
                                <icons:FluentIconElement Symbol="Dismiss20"/>
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </Grid.ContextFlyout>
                <Image Source="{x:Bind Clippy.CharacterImageSource, Mode=OneWay}" Height="100" Width="100"/>
            </Grid>
        </Canvas>
    </Grid>
</winuiex:WindowEx>



